#!/bin/sh
set -e

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd -W)"
if command -v pwsh >/dev/null 2>&1; then
  pwsh -NoProfile -File "$REPO_ROOT/scripts/bump-version.ps1"
else
  powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$REPO_ROOT/scripts/bump-version.ps1"
fi
git add app.js
